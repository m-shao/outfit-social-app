import LoginButton from '../components/LoginButton';
import Profile from './Profile';

function Auth() {
    return (
        <div>
            <LoginButton />
            <Profile />
        </div>
    );
}

export default Auth;
